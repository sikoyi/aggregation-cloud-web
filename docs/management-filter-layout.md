# 账号与设备筛选布局

范围：账号管理（含旧版平台账号列表）和设备管理。其余页面保持原样。

仅调整顺序，不增加分组标题、分隔线、折叠或改动字段名称。依次放置平台字段、账号字段、设备字段、状态与关联字段；跨页面相同字段保持相对顺序。新增字段保留在末尾，不能被隐藏。

保留每个字段的查询键、选项、远程依赖、持久化、自动查询、清空和权限规则，不删除业务上不同的条件。沿用现有紧凑网格及响应式布局。

验收：三个资源的字段集合和查询配置不变；平台字段顺序一致；配置输入顺序改变不影响布局；其他资源不重排。发版保持暂停，仅本地提交。
