# 对标账号资料独立选择

- 在账号监听的对标跟踪页新增昵称、简介、头像三个复选项。
- 新建默认全不选；从账号行编辑已有规则时回填保存值，旧接口已有规则缺少配置时按原来三项全选显示。
- 保留连续保存窗口和共用字段选项，清空上个账号及其对标主页；失败不清空。
- 帖子跟踪行为不变；本次不扩展对标平台，不改脚本端，不添加批量一次写入多账号。
- 权限沿用现有入口、账号选择范围和后端校验。
- 必须与支持 `profile_sync_fields` 的后端及 `20260914_0155` 迁移一起发布。不能单独更新前端，否则旧后端仍执行全量同步。
- 后端对未结束的资料同步任务返回 409，提示结束后再修改选项，不会假装已撤销旧任务。
- 测试：监听和总览两个测试文件共 15 项通过；类型检查及生产构建通过。
- 浏览器验证三项默认关闭、可分别勾选，以及 900×700 布局未溢出；未保存真实对标规则、未下发资料修改。
- 2026-09-14：用户确认昵称、简介、头像分别可选。
