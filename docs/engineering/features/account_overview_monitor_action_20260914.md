# 账号总览逐行监听设置

## 范围

- 用户确认本次仅增加逐行入口，不增加多选批量设置。
- 在账号数据总览的操作列保留查看详情，并增加带悬停提示的监听设置图标。
- 复用 `AccountDataView.vue` 的 `openMonitor(account)` 和现有弹窗、保存及关闭接口。
- 不改变账号访问权限、监听协议、后台调度、数据库或执行端脚本。
- 操作列固定 112px，两个按钮各 32px，避免互相挤压。

## 验收与验证

- 点击入口定位当前平台账号，留在总览，不直接启动监听。
- 已配置账号可打开保存配置；未配置账号显示确认开启，不残留上一账号身份。
- 取消后保持总览；保存错误仍沿用原弹窗反馈，不新增后台写入路径。
- `npm.cmd test -- src/views/AccountDataView.test.ts`：4 项通过。
- `npm.cmd run build`：类型检查和构建通过；依赖已有 PURE 注解警告。
- 本地浏览器验证已监听、未开启两类账号，确认弹窗标题与当前账号一致，操作列布局正常。
- 未提交真实监听配置、未触发采集；未部署或推送。既有本地 Vite 服务热更新生效，未重启后端。

## 回滚

- 撤销本次前端提交即可，不涉及数据迁移。
