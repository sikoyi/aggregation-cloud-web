# 监听统计卡片样式统一

- 范围：仅社媒账号数据顶部五张统计卡片，对齐外部账号监听；不改统计字段、接口、点击筛选逻辑或其他区域。
- 样式：最小高度 64px、内边距 12px、图标底座 32px、图标 20px、标签 12px、数字 20px；统一选中边框和键盘焦点。
- 响应式：大屏五列，1000px 以下三列，600px 以下两列；明暗主题沿用现有变量。
- 回归：`AccountDataView.test.ts` 固定关键样式和选中态；`smoke-monitor-summary-style.cjs` 挂载真实组件，对比两页计算样式并验证筛选、三种宽度和双主题。
- 交付范围：本地验证与提交，不推送、不发布生产。
