# 外部账号详情展示

2026-09-14 用户确认：保留列表弹窗入口，参照社媒账号数据的资料、指标和帖子展示，不改变外部账号采集业务。

- 顶部展示头像、昵称、公开用户名、简介、业务平台与监听状态；不添加内部账号的登录状态或绑定设备。
- 粉丝、关注、帖子数来源于账号资料；缺失值显示 `--`，真实零显示 `0`。本轮帖子及点赞评论合计只在采集记录表中明确标注本轮口径。
- 内容列表使用现有 `ContentPreview`，三行正文摘要、媒体预览、原帖链接、互动指标、发布和采集时间；展开可看完整正文、媒体和已采集评论。
- 弹窗仅保留一个主体纵向滚动区域，页脚刷新与关闭始终在外侧。窄屏表格在自身区域横向滚动。
- 沿用已有接口、权限、版本校验、请求序号保护与分页，不增加任何回复、账号绑定、任务下发操作。

验证：单元测试覆盖缺失数值、媒体 URL 安全过滤、完整正文、数据口径、详情翻页乱序响应和加载失败；使用本地完全模拟接口验证桌面、窄屏、评论展开与翻页。后端及脚本无需修改。
