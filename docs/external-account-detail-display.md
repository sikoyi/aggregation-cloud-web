# 外部账号详情展示

## 2026-09-20 列表头像昵称对齐

外部账号监听列表对齐社媒账号指标总览：头像固定 48px，昵称 16px、1.5 行高；保留原主页链接字号、列宽、详情样式和监听业务。文本区域可收缩，长昵称和链接继续单行省略。浏览器回归覆盖桌面、窄屏尺寸和长昵称边界，并保留浅色、暗色截图验收。

2026-09-14 用户确认：保留列表弹窗入口，参照社媒账号数据的资料、指标和帖子展示，不改变外部账号采集业务。

- 顶部展示头像、昵称、公开用户名、简介、业务平台与监听状态；不添加内部账号的登录状态或绑定设备。
- 粉丝、关注、帖子数来源于账号资料；缺失值显示 `--`，真实零显示 `0`。本轮帖子及点赞评论合计只在采集记录表中明确标注本轮口径。
- 内容列表使用现有 `ContentPreview`，三行正文摘要、媒体预览、原帖链接、互动指标、发布和采集时间；展开可看完整正文、媒体和已采集评论。
- 弹窗仅保留一个主体纵向滚动区域，页脚刷新与关闭始终在外侧。窄屏表格在自身区域横向滚动。
- 沿用已有接口、权限、版本校验、请求序号保护与分页，不增加任何回复、账号绑定、任务下发操作。

验证：单元测试覆盖缺失数值、媒体 URL 安全过滤、完整正文、数据口径、详情翻页乱序响应和加载失败；使用本地完全模拟接口验证桌面、窄屏、评论展开与翻页。后端及脚本无需修改。

## 评论与时间二次优化

展开后默认显示评论，完整帖文独立标签切换。评论展示作者头像（缺失或加载失败时首字占位）、作者、评论时间、正文、点赞和回复数，已知父评论显示回复作者，未采集父评论明确标注，不推测缺失原因。每页 10 条，保留平台总评论数与实际采集数的区别，不新增分页请求。发布时间和最近采集时间分别使用标签与值同行布局。

## 评论图片补齐

后端在既有 `platform_metadata` 中保留 `author_avatar_url`、`media_urls` 和 `media_only`，前端展示缩略图、放大预览及明确的加载失败状态。支持带图文字评论和纯媒体评论；视频地址按既有扩展名规则分辨。仅接受不含用户名密码的 HTTP(S) 地址，不输出原始 HTML。

旧记录缺失的图片地址无法在前端恢复，需等待后续采集返回。本次不自动触发真实账号采集，不改脚本端，不增加媒体代理或下载接口。上游未返回图片、地址过期或浏览器无法访问媒体站点时，仍不能保证图片可显示。

验收：后端转换测试、前端数据边界测试及构建通过；隔离浏览器验证头像与附图实际加载、放大预览、无效图片提示、桌面和 390px 窄屏展示。

## 粉丝数量缩写

外部监听列表、详情粉丝指标与采集记录粉丝列统一使用紧凑表示：1000 为 `1k`、12345 为 `12.3k`，百万及十亿分别使用 `m`、`b`，最多一位小数。点击数字弹出千分位完整数量，不改变列宽；按钮支持键盘操作。不足一千不缩写，缺失或无效数据为 `--`，零为 `0`。其他指标不变，不修改接口或存储值。数值边界、完整值保留与浏览器点击展示已验证。
