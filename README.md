# Aggregation Cloud Web

Vue3 + Pinia + Tailwind CSS + TypeScript 管理端，作为独立前端仓库维护。

## 本地运行

```bash
npm install
npm run dev
```

默认后端地址为 `http://127.0.0.1:8000`。需要调整时复制 `.env.example` 为 `.env` 并修改 `VITE_API_BASE_URL`。

## 默认测试账号

- 账号：`admin`
- 密码：`admin123456`
